import "dotenv/config";

async function checkApi() {
  const cookie = "TODO"; // Need to get a valid session cookie, maybe difficult.
  // Actually, I can just console log the error in the component.
}
